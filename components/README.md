# components (Active Runtime Path)

Status: Active runtime/provider surface.

This directory currently contains the canonical runtime provider and renderer used by the app runtime path.

Key files:
- MarveoProvider.tsx
- MarveoRenderer.tsx
- MarveoComponentRenderer.tsx

Phase 1 note:
- Preserve as source of truth while migration verification is ongoing.
