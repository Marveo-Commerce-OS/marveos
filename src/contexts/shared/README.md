# shared

Scope:
- Shared types, contracts, endpoint helpers, and utility primitives
- Cross-context helpers without domain ownership leakage

Planned migration sources:
- lib/types.ts
- shared helpers from src/lib/endpoints.ts and related utility modules

Phase 1 status:
- Boundary defined only. No code moved.
