# lib (Active Runtime and Core Services)

Status: Active core services and runtime utility surface.

This directory contains current operational logic for auth, API access, orchestration helpers, and runtime bundle clients.

Phase 1 note:
- Keep stable as implementation source while bounded contexts are introduced.
- Refactor work will happen behind compatibility layers in later phases.
